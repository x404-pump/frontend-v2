
export async function generateMetadata({ params }: { params: { id: string } }) {
}

export default async function Page({
    params,
}: {
    params: {
        id: string;
    };
}) {
    const { id } = params;

    return(
        <div>
            
        </div>
    )
}